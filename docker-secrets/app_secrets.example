# Example secrets file for git (DO NOT put real credentials here)
#
# Quick usage (local development):
# 1) Copy the example to the real secret file (local only):
#
#    cp docker-secrets/app_secrets.example docker-secrets/app_secrets
#
# 2) Edit `docker-secrets/app_secrets` and replace the CHANGEME values with
#    your real credentials.
#
# 3) Secure the file so only your user can read it:
#
#    chmod 600 docker-secrets/app_secrets
#
# 4) Start your stack (the secret file will be mounted into containers at
#    /run/secrets/app_secrets):
#
#    docker-compose up -d
#
# 5) Never commit the real `docker-secrets/app_secrets` file. The repo's
#    .gitignore already excludes `docker-secrets/` but tracks this example.
#
# Optional: securely remove the file when no longer needed (Linux):
#
#    shred -u docker-secrets/app_secrets
#

ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGEME
ADMIN_PASSWORD_ENCODED=CHANGEME
MEEXP_USER=user
MEEXP_PASSWORD=CHANGEME
